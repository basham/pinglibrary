package org.pingplatform.ping {

	public class PingSideData {
		
		public var bool:Boolean;

		public var percStart:Number; // .25
		public var percEnd:Number; // .65
		public var percCenter:Number; // .45
		public var percSize:Number; // .4
		
		// start, end, center, size
		
		public function PingSideData() {
		}

		//public function toString():String {
		//	return( 'RAW: ' + raw + ', VALUE: ' + value + ', MM: ' + mm + ', PERCENTAGE: ' + perc + ', BOOL: ' + bool );
		//}
		
	}
	
}